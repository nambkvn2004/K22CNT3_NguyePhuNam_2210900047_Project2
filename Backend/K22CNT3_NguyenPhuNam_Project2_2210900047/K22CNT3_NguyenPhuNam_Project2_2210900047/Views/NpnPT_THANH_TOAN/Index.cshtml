@model IEnumerable<K22CNT3_NguyenPhuNam_Project2_2210900047.Models.PT_THANH_TOAN>


@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Npn2210900047Layout.cshtml";
}

<div class="top-center-content">
    <h2>Index</h2>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
</div>

<table class="table">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.MaPTTT)</th>
            <th>@Html.DisplayNameFor(model => model.Ten_PTTT)</th>
            <th>@Html.DisplayNameFor(model => model.Trang_thai)</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.MaPTTT)</td>
                <td>@Html.DisplayFor(modelItem => item.Ten_PTTT)</td>
                <td>@Html.DisplayFor(modelItem => item.Trang_thai)</td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.MaPTTT }) |
                    @Html.ActionLink("Details", "Details", new { id = item.MaPTTT }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.MaPTTT })
                </td>
            </tr>
        }
    </tbody>
</table>

<style>
    .top-center-content {
        display: flex;
        flex-direction: column;
        align-items: center; /* Center items horizontally */
        text-align: center; /* Center text */
        margin: 20px 0; /* Optional: add margin above and below */
    }

    table {
        margin: 0 auto; /* Center the table */
    }
</style>
